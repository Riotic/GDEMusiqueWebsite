# Variables d'environnement pour Docker Compose
# Copier ce fichier vers .env et modifier les valeurs

# Base de données
POSTGRES_DB=gde_music
POSTGRES_USER=gde_user
POSTGRES_PASSWORD=gde_password_change_me

# Backend
SECRET_KEY=your-secret-key-here-generate-with-openssl-rand-hex-32
DEBUG=True
ALLOWED_ORIGINS=http://localhost:3000,http://localhost

# Ports (si vous voulez changer les ports par défaut)
FRONTEND_PORT=3000
BACKEND_PORT=8000
POSTGRES_PORT=5432
